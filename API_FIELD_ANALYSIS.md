# 新API字段 vs 数据库表字段对比分析

## 📊 字段映射关系

### ✅ 可以直接映射的字段

| 新API字段 | 数据库字段 | 说明 |
|---------|----------|------|
| `id` | `id` | ✅ 完全一致 |
| `product_id` | `product_id` | ✅ 完全一致 |
| `product_name` | `product_name` | ✅ 完全一致 |
| `carousel_pic_urls` | `image_list` | ✅ 需要转换：数组 → JSON |
| `sku_list` | `sku_list` | ✅ 完全一致（都是JSON） |
| `sale_count` | `soldcount` | ✅ 字段名不同，但含义一致 |
| `extcode` | `extCode` | ✅ 字段名大小写不同 |
| `create_time` | `create_time` | ✅ 完全一致 |
| `update_time` | `update_time` | ✅ 完全一致 |
| `infringement_num` | `infringement_index` | ⚠️ 字段名不同，含义可能相同 |

### ⚠️ 需要转换或处理的字段

| 新API字段 | 数据库字段 | 转换说明 |
|---------|----------|---------|
| `origin_product_url` | `url` | ✅ 直接映射 |
| `carousel_pic_urls[0]` | `main_image` | ⚠️ 需要取数组第一个元素 |
| `carousel_pic_urls[0]` | `cover` | ⚠️ 通常和main_image一致 |
| `product_name` | `title` | ⚠️ 可能需要映射到title |
| `product_name` | `name` | ⚠️ 可能需要映射到name |

### ❌ 新API有但数据库没有的字段

| 新API字段 | 说明 | 处理建议 |
|---------|------|---------|
| `group_id` | 分组ID | 可忽略或存储到其他字段 |
| `sku_specs` | SKU规格 | 可能对应`spec`字段 |
| `infringement_status` | 侵权状态 | 可能对应`infringement_index` |
| `ref_product_template_id` | 引用模板ID | 可能对应`isTemplateSku` |
| `ref_product_size_template_id` | 引用尺寸模板ID | 新字段，可忽略 |
| `is_publish` | 是否发布 | 可能对应`isupload`或`uploadstatus` |
| `master_user_id` | 主用户ID | 可能对应`user_id` |
| `create_by` | 创建者 | 新字段，可忽略 |
| `create_dept_id` | 创建部门ID | 新字段，可忽略 |
| `malls` | 商城列表 | 新字段，可忽略 |
| `product_template` | 商品模板 | 新字段，可忽略 |
| `product_size_template` | 尺寸模板 | 新字段，可忽略 |
| `group` | 分组信息 | 新字段，可忽略 |

### ❌ 数据库有但新API没有的字段

| 数据库字段 | 说明 | 处理建议 |
|----------|------|---------|
| `user_id` | 用户ID | 可能需要从`master_user_id`映射 |
| `goods_id` | 商品ID（数字） | 可能需要从`product_id`转换 |
| `title` | 标题 | 可能需要从`product_name`映射 |
| `name` | 名称 | 可能需要从`product_name`映射 |
| `create_time_str` | 创建时间字符串 | 可以从`create_time`转换 |
| `isupload` | 是否上传 | 可能需要从`is_publish`映射 |
| `uploadstatus` | 上传状态 | 新API没有，可能需要默认值 |
| `price` | 价格 | 新API没有，可能需要从其他地方获取 |
| `product_price` | 商品价格 | 新API没有 |
| `product_category` | 商品分类 | 新API没有 |
| `product_description` | 商品描述 | 新API没有 |
| `product_images` | 商品图片 | 可能对应`carousel_pic_urls` |
| `product_spec_map` | 规格映射 | 新API没有 |
| `sku2` | SKU2 | 新API没有 |
| `spec` | 规格 | 可能对应`sku_specs` |
| `upload_time` | 上传时间 | 新API没有 |
| `top_code` | 顶部代码 | 新API没有 |
| `top_msg` | 顶部消息 | 新API没有 |

## 🔍 关键差异分析

### 1. **图片字段**
- **新API**: `carousel_pic_urls` (数组)
- **数据库**: `image_list` (JSON), `main_image` (字符串), `cover` (字符串)
- **结论**: ✅ 可以映射，需要：
  - `carousel_pic_urls` → `image_list` (JSON字符串)
  - `carousel_pic_urls[0]` → `main_image`
  - `carousel_pic_urls[0]` → `cover`

### 2. **商品标识**
- **新API**: `product_id` (字符串，如 "601104322603708")
- **数据库**: `product_id` (字符串), `goods_id` (bigint，唯一索引)
- **结论**: ⚠️ 需要确认 `goods_id` 是否等于 `product_id` 的数字部分

### 3. **SKU相关**
- **新API**: `sku_list`, `sku_specs`
- **数据库**: `sku_list`, `sku2`, `spec`
- **结论**: ⚠️ `sku_specs` 可能对应 `spec` 字段

### 4. **用户相关**
- **新API**: `master_user_id`, `create_by`, `create_dept_id`
- **数据库**: `user_id`
- **结论**: ⚠️ `master_user_id` 可能对应 `user_id`

### 5. **上传状态**
- **新API**: `is_publish` (0/1)
- **数据库**: `isupload`, `uploadstatus`
- **结论**: ⚠️ 需要确认映射关系

## ✅ 总体结论

**核心字段基本一致，主要差异：**

1. ✅ **图片字段可以映射** - `carousel_pic_urls` → `image_list` + `main_image` + `cover`
2. ✅ **基础字段可以映射** - `id`, `product_id`, `product_name`, `sku_list` 等
3. ⚠️ **需要字段转换** - 一些字段名不同但含义相同
4. ⚠️ **新增字段可忽略** - 新API有一些数据库没有的字段，但不影响核心功能
5. ⚠️ **缺失字段需处理** - 数据库有一些新API没有的字段，可能需要默认值或从其他来源获取

**建议：**
- 核心功能字段（图片、商品信息、SKU）都可以正常映射
- 一些辅助字段（如`group_id`, `malls`等）可以忽略
- 需要确认`goods_id`和`product_id`的关系
- 需要确认`user_id`和`master_user_id`的关系
