FROM python:3.11-slim

WORKDIR /app

# 复制依赖文件
COPY backend/requirements.txt .

# 安装Python依赖（PyMySQL是纯Python实现，不需要系统MySQL库）
RUN pip install --no-cache-dir -r requirements.txt

# 复制应用代码
COPY backend/ .

# 暴露端口
EXPOSE 5000

# 启动命令
CMD ["python", "app.py"]
