server {
    listen 80;
    server_name _;

    # 前端静态文件
    location / {
        root /usr/share/nginx/html;
        index index.html;
        # 只有当文件不存在时，才回退到 index.html (支持 SPA 路由)
        # 但如果是 product_images 目录，应该直接由 root 映射
        try_files $uri $uri/ /index.html;
    }

    # 新增：映射本地图片目录
    location /product_images/ {
        root /usr/share/nginx/html;
        autoindex on;
    }

    # 设计图等上传目录（挂载在容器 /opt/images，避免与只读的 html 冲突）
    location /images/ {
        alias /opt/images/;
        add_header Cache-Control "public, max-age=3600";
    }

    # 后端API反向代理 - 使用resolver延迟DNS解析
    location /api/ {
        client_max_body_size 50m;   # 允许大 body（如 Base64 原图），避免 413
        resolver 127.0.0.11 valid=30s;
        set $backend http://backend:5000;
        proxy_pass $backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 增加超时时间，避免大文件上传超时
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 健康检查
    location /health {
        resolver 127.0.0.11 valid=30s;
        set $backend http://backend:5000;
        proxy_pass $backend/api/health;
        access_log off;
    }
}
