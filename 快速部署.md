# 🚀 快速部署指南

## 前置条件

1. ✅ 云服务器已购买并配置好
2. ✅ 数据库已迁移到云服务器（101.33.241.82:3307）
3. ✅ 服务器已安装 Docker 和 Docker Compose

## 一键部署

### 方法一：使用部署脚本（推荐）

```bash
# 1. 上传整个 goods_review_web 目录到云服务器
# 2. SSH 登录到云服务器
ssh user@your-server-ip

# 3. 进入项目目录
cd /path/to/goods_review_web

# 4. 运行部署脚本
chmod +x deploy.sh
./deploy.sh
```

### 方法二：手动部署

```bash
# 1. 进入 docker 目录
cd docker

# 2. 创建环境变量文件（如果不存在）
cat > .env << EOF
DB_HOST=101.33.241.82
DB_PORT=3307
DB_USER=root
DB_PASSWORD=你的数据库密码
DB_NAME=temu_baodan
SAVE_API_URL=http://temebaodan.all369.cn/api/pc/savegoods
AUTH_TOKEN=你的认证Token
EOF

# 3. 启动服务
docker-compose up -d

# 4. 查看服务状态
docker-compose ps
```

## 配置说明

### 必须修改的配置

编辑 `docker/.env` 文件，修改以下值：

- `DB_PASSWORD`: 数据库密码
- `AUTH_TOKEN`: 外部API认证Token（默认值：`d6439e3f68072e610aedb646f0589717cb54061d4b336b94fbe73be79886a24d`）

### 端口配置

- **前端**: `8080` 端口
- **后端**: `5001` 端口

如需修改，编辑 `docker/docker-compose.yml`

## 访问地址

部署完成后，访问：

- **前端**: `http://your-server-ip:8080`
- **后端API**: `http://your-server-ip:5001/api/health`

## 防火墙配置

确保云服务器防火墙/安全组开放以下端口：

- `8080` (前端)
- `5001` (后端)

## 验证部署

```bash
# 检查服务状态
cd docker
docker-compose ps

# 查看日志
docker-compose logs -f

# 测试后端
curl http://localhost:5001/api/health

# 测试前端
curl http://localhost:8080
```

## 常见问题

### 1. 数据库连接失败

检查 `docker/.env` 中的数据库配置是否正确。

### 2. 端口被占用

修改 `docker/docker-compose.yml` 中的端口映射。

### 3. 无法访问

- 检查防火墙规则
- 检查安全组设置
- 查看服务日志：`docker-compose logs`
